{
  "name": "mcp-server-example",
  "version": "1.0.0",
  "mcpServers": {
    "nodeServer": {
      "command": "ts-node",
      "args": ["scripts${/}start.ts"],
      "cwd": "${extensionPath}"
    }
  }
}
